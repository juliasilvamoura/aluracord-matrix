yarn para react nativo é melhor tem que instalar, o npm já vem nativo do node.js 
precisa criar o .json com npm init
precisa criar o lock com npm install next react react-dom 
para Starta: npm run dev (o dev está dentro do package.json)

// "500": "#3F9142", cor inicial